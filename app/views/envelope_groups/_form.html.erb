<%= form_for(envelope_group) do |f| %>
  <% if envelope_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(envelope_group.errors.count, "error") %> prohibited this envelope_group from being saved:</h2>

      <ul>
      <% envelope_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %>
    <%= f.text_field :account_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
